Codebase uses TypeScript with 2-space indentation, semicolons, double quotes, and trailing commas where multiline. Comments are sparse and mostly short Chinese explanations above logical blocks/functions. Prefer small helper functions, explicit typing, and simple control flow over clever abstractions. UI text mixes English labels with some Chinese descriptions. Follow existing naming: camelCase for functions/variables, PascalCase for provider/model types, exported command IDs/constants in extension-oriented style.