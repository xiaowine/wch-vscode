Windows project commands:\n- `pnpm run compile` — TypeScript compile to out/\n- `pnpm run lint` — run ESLint on src\n- `pnpm run test` — run VS Code extension tests\n- `pnpm run watch` — TypeScript watch mode\n- `pnpm run vscode:prepublish` — compile before packaging/publishing\nUseful shell tools in this environment: `git --no-pager status`, `Get-ChildItem`, `rg <pattern> src`, `pnpm <script>`. Repo root is `C:\\Users\\xiaow\\Desktop\\test`.